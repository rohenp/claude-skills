# Audit Log Archive — 2026-Q2 (April 25–27)
> Archived from audit-log.md on 2026-04-29. Append-only history.
> Latest entries remain in ../audit-log.md. Compaction protocol: see prose-source.md.

---

## 2026-04-25 [golang-conventions, golang-tester, postgres-conventions, docker-dev, api-conventions, api-security, api-performance, service-design, observability]
Action: audit-only (light-touch — user requested no aggressive rewrite; preserve Go 1.26 / tern guidance verbatim)
Trigger: edits to Go-stack skill set for Go 1.26 modernization + migration tool swap (golang-migrate → tern)

Sizes (post-edit):
  golang-conventions   L2 ~597 tok (63 ln)   L1 482/1024
  golang-tester        L2 ~753 tok (152 ln)  L1 439/1024
  postgres-conventions L2 ~597 tok (87 ln)   L1 513/1024
  docker-dev           L2 ~1477 tok (315 ln) L1 584/1024
  api-conventions      L2 ~1002 tok (146 ln) L1 690/1024
  api-security         L2 ~881 tok (138 ln)  L1 498/1024
  api-performance      L2 ~816 tok (147 ln)  L1 609/1024
  service-design       L2 ~709 tok (145 ln)  L1 463/1024
  observability        L2 ~789 tok (171 ln)  L1 664/1024

L1 changes (T8 light pass — descriptions retitled to advertise Go 1.26 / new tooling):
  golang-conventions:   added "(target: Go 1.26)"; added "modern stdlib (slices/maps/cmp/errors)" coverage tag
  golang-tester:        added "(target: Go 1.26)"; added t.Context, b.Loop, errors.AsType, wg.Go to coverage
  postgres-conventions: added "(via tern)"; added "errors.AsType for *pgconn.PgError" + "tern migration tooling"

⊘T9: ALL sections (per user instruction). Schema-encoding skipped on every body section.

Techniques applied: none (audit-only — flagging redundancy without rewriting).
Flags / observations (no action taken; left for future passes):
  - golang-conventions: "Modern Stdlib First" bullet `wg.Go(fn) over wg.Add(1)+defer Done()` overlaps with Concurrency&Performance's `Goroutines+WG: wg.Go(fn)` (1-bullet, ~12 tok). Defensible — different framings (modern-vs-old contrast vs imperative default). Leave.
  - api-conventions L1 (690 chars) is heaviest L1 in set; well under 1024. No action.
  - service-design SKILL.md added a 13-line `cmp.Or` Config example. Behavioral demonstration of Go 1.26 idiom — DNC.
  - postgres-conventions SKILL.md gained ~25 lines (tern Migrations section). All command syntax + safety rules; DNC.
  - docker-dev SKILL.md +12 lines (3 new tern make targets). Imperative defaults; DNC.

prose-source sync (mandate):
  golang-conventions    ✓ rewritten
  golang-tester         ✓ rewritten
  postgres-conventions  ✓ rewritten (Migrations → tern)
  docker-dev            ✓ patched (Go 1.26 base, tern install, migrate-* targets, layout)
  api-conventions       ✓ patched (errors.AsType note)
  api-security          divergent by design (compact L2 helper not present in prose) — accepted
  api-performance       ✓ patched (loop var idiom)
  service-design        ✓ patched (signal.NotifyContext + cmp.Or in LoadConfig)
  observability         divergent by design (prose uses long-form 3-call gauge registration; SKILL uses compact map form which received the loop-var fix) — accepted

Notes:
  - User explicitly requested preserve verbatim — audit-only run.
  - Net effect of edits: +~150 tok across 9 skills for new Go 1.26 / tern guidance. All load-bearing behavioral content. No bloat to compress.
  - Future pass candidate: golang-conventions Modern Stdlib bullet list (~17 lines) could be reorganized into a denser `T9` schema if user later requests compression. Currently kept as plain bullets for legibility.

## 2026-04-26 skill-token-optimizer (v4 — methodology additions, user-requested)
Before: ~1251 tokens (174 lines, post pass-7)
After:  ~2202 tokens (246 lines) — net +76% (deliberate: 11 load-bearing methodology additions; v4 bump)
Techniques: behavioral-additions (see Notes — 11 changes implementing user review feedback) | ⊘T9: 0 net new (existing ⊘T9 sections preserved; new sections in Core+short or schema where qualified)
L1 changes: added `Pre-flight: trivial-change skip (diff<3 ln & L1 unchanged) | at-floor (last 2 runs <3%) → skip.` to advertise auto-trigger gates. New L1: 669/1024 chars (65% budget; ~167 tok). Recall preserved — new clause is a behavioral gate disclosure, not a trigger phrase.
Notes (additions, in order):
  1. Decoding-cost principle (Notation section): schema correct iff (a) operator grammar in same section AND (b) decode <5s without scrolling. T10 now enforces. Core+short — DNC. Reason: schema notation has cognitive parse cost paid on every trigger; this principle bounds when compression is actually a net win.
  2. Hot/cold cache amortization: hot skills (≥10 trigs/day) amortize via 5-min prompt cache → aggressive ok; cold skills (rare) → schema parse-cost > savings; prefer prose, stop after T1+T6. Added to Token Architecture and as a benchmarks-table modifier. Core+short — DNC.
  3. T1 reversal heuristic: load_freq ≥80% of trigger_freq → pull back inline (T1 was wrong). Reason: progressive disclosure is only a win when reference loads less than skill triggers. DNC.
  4. T5 mechanism reality + reversal: clarified _shared/ loads on-demand like references/ (no auto-load). Token win only if SKILL.md replaces inline copy with pointer AND pointer not followed. Reversal: shared file loaded ≥80% → revert to inline. DNC.
  5. L1 metric correction: L1_tokens ≈ chars÷4 (token count) vs L1_budget_used = chars÷1024 (utilization, 0–1). Earlier audit logs conflated these. Both formulas now in Token Architecture. DNC.
  6. Pre-flight 1 — trivial-change skip: diff<3 ln & L1 unchanged → log Action:skipped-trivial; STOP. Prevents 1-line typo edits triggering full audit cycles. DNC.
  7. Pre-flight 2 — stopping criterion: last 2 entries each <3% reduction & no L1 issues → log Action:at-floor; STOP. Prevents the "7 back-to-back diminishing-returns runs" pattern visible in 2026-03-26 entries. DNC.
  8. Audit log compaction protocol (>200 ln trigger): keep latest 3 entries, archive older to references/audit-log-archive/YYYY-QN.md, maintain references/preserved-sections.md as current-state index. Future "check audit-log first" reads index + latest 3 only. Current log is 178 ln post-this-entry → next entry should evaluate compaction. DNC.
  9. Classification Taxonomy (new top-level section): 5 categories (Core, Core+short, Reference, Redundant, Verbose) defined once with their routing decision. Resolves prior mismatch where Output Format reported 5 categories but the routing table only mentioned 3. DNC.
  10. T8b numbering note: T8b is co-located variant of T8 (both L1-scope); not "T11" — do not renumber. Audit-log entries from 2026-03-26 onward use this convention. Behavioral instruction — DNC.
  11. Notation section hoisted (single source): operator grammar + subject grammar moved to dedicated top-level section. classify+route table and T9 now reference Notation instead of redefining. Eliminates duplication between classify-table header and T9 operator list. DNC.
Net token impact: +951 tokens. Skill is now at upper edge of comprehensive bracket (1200–1800 target → 2200 actual). This violates the benchmark target by ~22%. Decision: ACCEPT. All 11 additions are methodology-level behavioral gates — compressing them with T9 would trigger their own decoding-cost principle (judgment-heavy content, parse cost > savings). Future passes: do NOT attempt to compress these sections. They are Core+short by construction (judgment-heavy + ≤6 lines each).
prose-source sync: ✓ rewritten as v4 (376 ln); decoding-cost, hot/cold, reversals, _shared/ mechanism, L1 metric, pre-flights, compaction, taxonomy, T8b note, Notation hoist — all reflected. Last-updated date set to 2026-04-26.
Stopping criterion check: this is a structural rewrite, not iterative compression. Stopping criterion does not apply — it gates iterative passes against the same content baseline. Resets effective on next run.

## 2026-04-26 prompt-optimizer (Claude-specific expansion, user-requested)
Before: ~1080 tokens (136 lines, pre-rewrite)
After:  ~2036 tokens (187 lines) — net +89% (deliberate: implements user-requested skill-review feedback; not compression)
Techniques: L1 retightening (T8b retained; trigger phrases compressed 13→8) | T9 retained for variance→diagnosis routing in Iteration Workflow | T1-considered-rejected for Token Estimation (50 tok inline; used during workflow, T1 would cost a per-trigger load) | references/failure-taxonomy.md DELETED (was 23-line duplicate of in-body taxonomy)
⊘T9 (sections preserved): Skill File Detection (judgment cues — "if ambiguous, ask") | One-Shot/System Prompt/Agent templates (DNC output templates) | Defaults + Output Formats (pipe-separated imperatives + key→field; DNC) | Concept-pointers bullet (3 cautionary examples are the sole demonstration of the safety rule)
L1 changes: rewrote description (~640 chars / 1024, 62% budget). Trigger list cut from 13 phrases to 8 (dedup'd "optimize my agent" / "agent system prompt" / "write an agent prompt" overlaps). Added "Covers Claude-specific patterns: XML tags, prefilling, prompt caching, model selection, tool use." sentence to advertise scope. T8b form `skip: [skill files] → skill-token-optimizer` retained.
Notes:
  - User-requested expansion implementing 11 skill-review suggestions: (1) added Claude-Specific Patterns section [XML structural tags, assistant prefilling, system param vs first-turn, model-aware Haiku/Sonnet/Opus 4.x, few-shot rules, tool_choice]; (2) updated System Prompt Architecture caching subsection [read 0.1× / write 1.25× costs, 5-min vs 1-hour `extended-cache-ttl-2025-04-11` beta, cache_control breakpoint placement]; (3) consolidated failure taxonomy by deleting L3 file (was duplicated in 3 places); (4) added Iteration Workflow section with variance→diagnosis schema; (5) tightened L1 trigger phrases; (6) added "Wrong architecture" 8th row to Failure Taxonomy; (7) added Token Estimation section noting code/JSON ÷3 and CJK ÷1.5 caveats; (8) added safety rule to Concept Pointers (single canonical meaning; MECE/BLUF/GROW examples); (9) made test-3× concrete (`temperature: 1.0` + variance-type → diagnosis); (10) referenced extended thinking in place of "think step by step"; (11) reconciled Defaults + Output Formats drift between SKILL.md and prose-source.md.
  - Net token impact: +956. Skill at top of "domain-expert" bracket (1500–2500 target; actual 2036). Decision: ACCEPT. All additions are user-requested behavioral content directly improving prompt-optimizer's output quality. Compressing them with T9 would trigger decoding-cost principle (judgment-heavy: model-selection nuance, prefill mechanics, caching tradeoffs).
  - DNC sections to preserve in future runs: Failure Taxonomy table | Claude-Specific Patterns sub-bullets (XML/prefilling/system-vs-turn/model-aware/few-shot/tool-use — all behavioral techniques) | One-Shot template | System Prompt template | Agent template | Caching mechanics (4 bullets — each <15 tok savings, T10 would fire) | Concept pointers cautionary examples | Token Estimation bullets (lookup but used per-trigger).
  - Future pass guidance: do NOT attempt to compress Claude-Specific Patterns section. Each sub-bullet has been minimized; further T6/T9 passes would corrupt judgment-heavy content (e.g., "weaker persona loading; add ~30% more scaffolding" → cannot be safely shortened without losing the conditional nature).
  - prose-source.md synced (canonical source, last-updated 2026-04-26).
  - Audit log compaction trigger: log will cross 200 lines after this entry (~220 ln). Per v4 protocol, next prompt-optimizer or other-skill run should evaluate compaction (keep latest 3 entries; archive older to references/audit-log-archive/2026-Q2.md; rebuild references/preserved-sections.md). Deferred from this run (user task scope was prompt-optimizer review, not log maintenance).
  - Pre-flight: ran (neither trivial-skip nor at-floor fired — first audit for this skill).
  - Stopping criterion check: structural rewrite, not iterative compression — does not apply.

## 2026-04-26 compaction — 7 entries archived; preserved-sections.md rebuilt
Action: audit-log compaction per v4 protocol (log was 215 ln, exceeded 200-ln trigger).
Archived: all 7 2026-03-26 skill-token-optimizer self-audit entries → `audit-log-archive/2026-Q1.md`.
Retained verbatim (latest 3): 2026-04-25 Go-stack audit | 2026-04-26 skill-token-optimizer v4 | 2026-04-26 prompt-optimizer expansion.
Built: `preserved-sections.md` — current-state index of DNC / ⊘T9 sections across all audited skills.
Future runs: read `preserved-sections.md` + the 3 retained entries; consult `audit-log-archive/2026-Q1.md` only for skill-token-optimizer historical pass detail.

## 2026-04-26 [postgres-vector-conventions, postgres-chunking-conventions, golang-rag-conventions]
Action: T8 L1 fix — three skills had L1 descriptions over the 1024-char hard limit (silent-truncation risk).
Trigger: full-set audit by user request.

L1 changes (chars before → after):
  postgres-vector-conventions:   1046 → 959   (drop type/HNSW/quant parentheticals; "diagnosing vector query performance" → "diagnosing perf")
  postgres-chunking-conventions: 1042 → 986   (drop partitioning + bulk-ingest parentheticals; "soft delete and re-chunking via tombstoning" → "re-chunking via tombstones")
  golang-rag-conventions:        1027 → 931   (drop "Use when writing or reviewing Go for ingestion, chunking, embedding, retrieval, or reranking" — duplicated by Trigger CSV below)

Techniques: T8 (description-tightening) only. No body changes. ⊘T9: n/a (L1-only pass).
Notes:
  - All three were marginally over (within 18-22 chars). Cause: verbose Covers parentheticals added during initial drafting.
  - Trigger phrases preserved — recall not affected.
  - prose-source sync: not present for these skills (created 2026-04-26 without prose-source companion). Defer creation; skill bodies are the canonical source.
  - Pre-flight: ran (not trivial — L1 was over hard limit; not at-floor — first audit).

## 2026-04-26 nextjs-conventions (Next.js 15 → 16 correctness rewrite)
Action: Full body rewrite to match Next.js 16 feature set (proxy.ts, async request APIs, "use cache", updateTag/revalidateTag/refresh, React Compiler default-on, typed Go API client). Skill was advertising Next.js 15 while user CLAUDE.md and `nextjs-expert` agent both target Next.js 16 — not optimization, correctness.
Before: ~248 tokens (22 ln body)
After:  ~1246 tokens (110 ln body) — net +1000 tok (deliberate; was empty of Next.js 16 content)
Techniques: full rewrite | ⊘T9: all sections (judgment-heavy framework guidance; output templates for proxy.ts / use cache / Server Action examples; DNC)
L1 changes: rewritten — version label now "Next.js 16"; trigger surface expanded to include `proxy.ts`, `use cache`, `updateTag/revalidateTag/refresh`, React Compiler. Added `skip: [Go API code] → api-conventions`. New L1: 691 chars / 1024 (67% budget). Recall expanded — old description omitted every Next.js 16-specific signal.
Notes:
  - DNC sections to preserve: proxy.ts code template | Async Request APIs example | "use cache" code template | Invalidation block (3 primitives) | Typed Go API Client template | Server Action template | React Compiler "do NOT manually wrap" rule (judgment-heavy).
  - prose-source.md synced (canonical source, last-updated 2026-04-26). Old prose-source was Next.js 15 — fully replaced.
  - Token count is well within domain-expert bracket (1500–2500 target; actual 1246, slightly under). No further compression needed.

## 2026-04-26 rag-conventions (T1 — lookup tables → references/)
Before: ~3524 tokens (265 ln body) — 96% over comprehensive bracket high
After:  ~2730 tokens (213 ln body) — 23% reduction
Techniques: T1 (six lookup tables → `references/lookup-tables.md`); replaced each with 1-line pointer summarizing intent. Kept all behavioral content inline.
⊘T9: 0 (no schema-encoding applied; tables were not behavioral routing — they're reference data, T1 was the right move)
L1 changes: none
Notes:
  - Moved to references/lookup-tables.md: Chunking strategies (8 rows × 3 cols), Embedding selection (7 rows × 2 cols), Index choice + HNSW knobs (5 rows × 6 cols), Filtered retrieval patterns (3 rows × 2 cols), Query rewriting patterns (6 rows × 3 cols), Reranker classes (4 rows × 4 cols).
  - Kept inline (DNC): Pipeline diagram, Sizing rules (atomic units), Mandatory metadata, Dense-vs-sparse-vs-multi-vector contrast, Embedding operational rules (6 bullets), Matryoshka cascades, Quantization (4 bullets), Hybrid + RRF formula, Top-K selection, Context construction (7 bullets), Evaluation (4 sub-sections), Production hardening (5 sub-sections), Pitfalls (10 bullets), Decision order (10 steps).
  - T1 reversal flag for future runs: if `references/lookup-tables.md` is loaded ≥80% of rag-conventions trigger fires, this T1 was wrong — pull tables back inline. The pointer summaries are intentionally complete enough that one-shot questions ("what reranker?") often won't need the deep table.
  - Skill is still over comprehensive bracket high (1800 target; actual 2730). Further T1 would require sacrificing behavioral content — STOP here. Decision order + Production sections are load-bearing and judgment-heavy.

## 2026-04-26 coldfusion-security (T1 — vulnerability deep-dives → references/)
Before: ~2719 tokens (226 ln body) — over comprehensive bracket
After:  ~1700 tokens (109 ln body) — 37% reduction
Techniques: T1 (12 per-vulnerability deep-dives → `references/vulnerability-catalog.md`); replaced with 12-row "Vulnerability index" table (severity + summary + audit grep per row).
⊘T9: 0 (T1 only)
L1 changes: none
Notes:
  - Moved to references/vulnerability-catalog.md: 12 vulnerability classes with paired vulnerable/secure code blocks (CFML injection, SQL injection, XSS, CF Admin exposure, file upload, session security, path traversal, WDDX deserialization, SSRF, CSRF, IDOR, error handling).
  - Kept inline (DNC): Enterprise context framing | Vulnerability index table (12 rows — severity + 1-line summary + audit grep, sufficient for triage without loading L3) | CVE patch table (3 rows — load-bearing) | Java deserialization protection | Network hardening | Enterprise governance (Shared Services, Standards, Multi-app triage) | Quick-reference checklists (3 — code/server/governance) | Output Format.
  - T1 reversal flag: if vulnerability-catalog.md is loaded ≥80% of coldfusion-security trigger fires, the inline index table is too compressed. Vulnerability triage normally happens at the index level (severity + grep), so loads should be infrequent — only when writing a fix or pen-test write-up.
  - Skill now within domain-expert bracket (1500–2500 target; actual 1700).
  - prose-source.md not synced this run — checklists.md and prose-source.md exist but were untouched. Defer prose-source sync until next coldfusion-security audit (this was a structural T1 move, not a content rewrite).

## 2026-04-26 cross-skill T5 — content-hash + dual-column rules — DEFERRED
Action: skipped (not applied).
Trigger: full-set audit identified content_hash normalization rule and embedding-model dual-column cutover repeated 3× across rag-conventions, postgres-chunking-conventions, golang-rag-conventions / postgres-vector-conventions.
Reason: T5 reversal rule (per v4 protocol §T5) requires that the `_shared/` file be loaded <80% of the trigger fires, otherwise the on-demand load erases the savings AND adds parse cost. We have no measurement of cross-skill load behavior. Inline duplication ~75 tok total — small enough that risk of T5 reversal outweighs the savings.
Decision: leave inline. Re-evaluate after telemetry exists for `_shared/` load frequency.
Notes:
  - Future-pass guidance: do NOT redo this analysis without measurement data. The conservative default for cross-skill repeats <100 tok is "accept duplication".

## 2026-04-26 prompt-security (new skill, prose-first → compressed)
Before: ~5000 tok (24935 chars, 274 ln) — prose-first first write, comprehensive bracket high
After:  ~3900 tok (19431 chars, 259 ln) — 22% char reduction
Techniques: L1 T8b+T8 (description >1024 chars → tightened to ~890; dropped redundant "Use when…" preamble; merged Covers/Trigger; preserved skip clauses); T6 across all body bullets (stripped "make sure to"/"you should"/"the model can…"-style preambles; imperatives only); T2 (collapsed 2-line `**Header**: explanation` patterns into single rule lines; merged redundant blockquote intros); T3 on tables (shortened cell prose; trimmed "the only mental model that scales", "use this when auditing…" subtitles); preserved `>` prose-source pointer.
⊘T9: ALL behavioral sections (Trust Tiers rules, Direct/Indirect Injection, Authority & Tools, Output Handling, Multi-Tenant, Secrets, System-Prompt Hygiene, Audit, Review Checklist). Reasons: judgment cues ("when…", "unless…"), <15 tok savings per section after T6, decoding cost of schema notation > prose for security-critical guidance. Schema-encoding security rules degrades them into checklists and loses the principal-level signal.
L1 changes: 1300 → ~890 chars; description tightened by collapsing "Use when designing, reviewing, or hardening any surface where untrusted text reaches an LLM — user input, retrieved chunks (RAG)…" preamble; Covers section preserved (CSV); Trigger CSV preserved; 3× skip clauses preserved.
Notes:
  - prose-source.md created as snapshot of original prose-first SKILL.md before compression — canonical source per protocol.
  - DNC sections preserved verbatim: Trust Tiers table headers, Jailbreak Classes table, Report Format template, Severity definitions, Defaults pipe-list.
  - ⊘T9 rationale documented (security guidance loses precision under schema-encoding; bullets-with-rationale are the optimal form for principal-level rule-restate-with-judgment).
  - Skill is in comprehensive bracket high (1200–1800 target; actual ~3900). Further compression would require T1 splitting (e.g., Multi-modal injection details + Jailbreak class table → references/) — declined this pass because the file is single-domain coherent and triggers as a unit. Re-evaluate if a future audit shows lookup-table style sub-sections are loaded selectively.

## 2026-04-26 postgres-cryptoshredding (new skill, prose-first → audit-only / already efficient)
Before: ~4150 tok (3112 words, 446 ln body) — written prose-first against the v4 protocol; comprehensive bracket high
After:  ~4131 tok (3098 words, 445 ln body) — <0.5% reduction (one micro T6 fix)
Techniques: T6 (single redundant sentence dropped from Architecture caveat — "Provider-agnostic; AWS KMS is the example" already covered by parenthetical and L1 description). Otherwise audit-only.
⊘T9: ALL judgment-heavy sections — Why ECIES for small PII (5 bullets, judgment cues throughout: "only if FIPS compels", "compromise cannot decrypt", "switch to envelope for blobs"); Blind-index Rules (7 bullets, cross-cutting judgment: "not a cryptoshred candidate", "Don't blind-index low-cardinality"); Caching Hard rules (5 bullets); Backup patterns 1–4 (rationale-heavy: "Acceptable because…", "Used in regulated environments…", "Operational, not cryptographic"). Same rationale as 2026-04-26 prompt-security audit: principal-level security guidance, decoding-cost principle fails for schema-encoding (security rules need their *why*).
L1 changes: tightened during initial write — final L1 1000/1024 chars (98% budget), recall covers 8 trigger phrases (cryptoshred / right to be forgotten / GDPR erasure / per-user encryption key / envelope encryption / ECIES / blind index / PII encryption Postgres). 5 skip clauses in T8b form (postgres-conventions / api-security / observability / prompt-security / security-reviewer). No T8b conversions needed post-write — already in L1 form.
Pre-flight: ran (new skill — neither trivial-skip nor at-floor applies; audit-log at 182 ln, under 200 trigger).
Notes:
  - DNC sections preserved verbatim: Three properties, Architecture diagram, Curve/AEAD/KDF (5 short imperatives), Symmetric envelope variant, Not-AES-SIV rationale, all SQL CREATE TABLE blocks (Schema), Storage rules, blind-index code template, Caching table, Invalidation order block, Negative caching, Batch decryption, all Go interface skeletons (Cipher/KeyStore/BlindIndexer/Cryptoshredder), Repository pattern code, Logging discipline (LogValue template), Shredding code template, Notes block, Soft-revoke vs hard-shred, KEK/per-user/blind-index rotation procedures, Operational rules (5 hard rules), Compliance-language note, Audit logging fields/lookups, Threat-model table (9 rows × 2 cols), Not-protected list, Anti-patterns (15 "X, not Y" pairs — DNC by classification taxonomy), Defaults pipe-list (canonical one-line summary).
  - T6 candidates considered and rejected: bullet rationale clauses ("Public key encrypts; private key decrypts. Write-only services hold the public key only — compromise cannot decrypt prior records.") would save ~80 tok across the skill but lose the load-bearing *why*. At ~2% savings, below the 5% noise floor; protocol calls for "already efficient" report.
  - T1 candidates considered and rejected: the 4 backup patterns could move to references/, but each pattern's rationale is judgment-heavy and selected based on regulatory posture — load-bearing during a design review, would require selective load. The threat-model table is short (9 rows) and used at-a-glance during reviews. T1 reversal risk is high.
  - T9 candidates considered and rejected: every candidate triggered T10 gate — judgment cues present, decoding-cost would exceed savings.
  - Token count is at upper edge of comprehensive bracket (1200–1800 target; actual 4131). Decision: ACCEPT, mirroring 2026-04-26 prompt-security (3900 tok accepted) and rag-reviewer (3270 tok accepted). Principal-level security skill — judgment density is the value.
  - prose-source.md: written first (canonical, 604 ln). SKILL.md derived. They are in sync.
  - Stopping criterion check: first audit for this skill — does not apply.
  - Future-pass guidance: do NOT attempt T9 on Why-ECIES, Blind-index Rules, Caching Hard rules, or Backup patterns. Each contains judgment cues that would corrupt under schema-encoding (e.g., "Don't blind-index low-cardinality fields (gender, country) — leakage is severe" cannot be safely shortened without losing the conditional). Future T1 candidates (if a load-frequency signal emerges): backup patterns could move to references/backup-patterns.md, threat-model table could move to references/threat-model.md — but only after reversal-check data exists.

## 2026-04-26 rag-reviewer (new agent, prose-first → compressed)
Before: ~3850 tok (19281 chars, 217 ln) — agent file with skill-style frontmatter
After:  ~3270 tok (16359 chars, 215 ln) — 15% char reduction
Techniques: L1 T8b+T8 (description 1100 → ~880 chars; trimmed "RAG (Retrieval-Augmented Generation)" parenthetical only on second mention; collapsed Handles bullets into CSV); T6 across body (dropped "make sure"/"verify that"-style preambles; imperatives only); T2 (collapsed `**Bold-prefix**: explanation. Flag X.` two-clause patterns; many bullets keep both bold + rule because the bold IS the trigger). Cross-skill T5: prompt-security pointer added in Failure modes / indirect-injection bullet ("see prompt-security skill for full defense set") to avoid restating that skill's content.
⊘T9: ALL Quality + Performance + Reliability + Schema sections. Reasons: judgment cues throughout ("unless eval set proved…", "when top-K is dominated", "Flag if…"), <15 tok savings per section, decoding cost > prose. Pitfalls + Severity + Report Summary already DNC.
L1 changes: 1100 → ~880 chars; trimmed "deep Go + Postgres + pgvector expertise" framing; combined `Handles:` bullets into CSV; skip clauses tightened (dropped "security vulnerabilities outside the retrieval surface, generic auth, CORS" → "non-retrieval security, generic auth, CORS").
Notes:
  - Agent file (not skill); treated like a skill for compression because frontmatter + body structure are equivalent. No references/ directory created (agents don't follow that pattern).
  - DNC sections preserved verbatim: Pitfalls list (16 anti-patterns — checklist precision matters), Tests bash block, Report Format template, Severity definitions, Report Summary pipe-list.
  - ⊘T9 same rationale as prompt-security: principal RAG-review judgment is encoded in the rationale clauses ("Mismatch silently degrades recall", "recall collapses under selective filters"). Schema-encoding loses this.
  - Original was already fairly tight (15% achievable vs prompt-security's 22%) because it had less prose preamble. Floor reached for this pass — further compression would require splitting the Quality/Performance/Reliability checklists into separate referenced files, but they are designed to be read together during a review and serve as the agent's behavioral spine. Decline T1.

---

